export type Anime = {
    name: string,
    url: string,
    image_url: string,
    anime_type: string,
    rating: string,
    episodes: string,
    description: string,
}

export type User = {
    name: string | null,
    image_url: string | null,
}